foscam_off:
  sequence:
  - service: switch.turn_off
    data:
      entity_id: switch.cam_salon_motion
foscam_on:
  sequence:
  - service: switch.turn_off
    data:
      entity_id: switch.cam_salon_motion
  - service: switch.turn_on
    data:
      entity_id: switch.cam_salon_motion
